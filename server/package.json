{
  "name": "smartvcard-deploy-api",
  "version": "1.0.0",
  "description": "API server for deploying Smart vCards to subdomains",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "vcard",
    "deployment",
    "express",
    "api"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.0.1"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
