# Frontend Dockerfile for Smart vCard (Nuxt.js SPA)
FROM node:24-alpine

WORKDIR /app

# Copy package files
COPY package.json yarn.lock ./

# Install all dependencies (needed for build)
RUN yarn install --frozen-lockfile

# Copy source code
COPY . .

# Build the application (keeps .nuxt directory)
RUN yarn build

# Expose port
EXPOSE 4645

# Set environment variables
ENV HOST=0.0.0.0
ENV PORT=4645
ENV NODE_ENV=production

# Start Nuxt in production mode
CMD ["yarn", "start"]
